# Copy this file to .env.local and fill in your values.
# Never commit .env.local to git.

# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://bounty_user:joyboy@localhost:5432/bounty

# JWT secret — used to sign and verify auth tokens
# Generate one with: openssl rand -base64 32
JWT_SECRET=8vToqiL53gJBqMneLAJmDEIV+ZsDvusdo7lrR9s3pNw=

# App URL (used for redirects in production)
NEXT_PUBLIC_URL=http://localhost:3000
