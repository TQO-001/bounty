generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Job {
  id          Int      @id @default(autoincrement())
  company     String
  position    String
  dateApplied String
  status      String
  notes       String?
  createdAt   DateTime @default(now())
}
